/**
 * 应用共享枚举定义。
 * TODO: 后续按模块拆分为更细的状态枚举文件。
 */
export enum AppTab {
  PLAN = 'PLAN',
  TODO = 'TODO',
  FOCUS = 'FOCUS',
  IDEA = 'IDEA',
  USER = 'USER',
  TEST = 'TEST'
}

export enum ThemeMode {
  LIGHT = 'LIGHT',
  DARK = 'DARK',
  SYSTEM = 'SYSTEM'
}
